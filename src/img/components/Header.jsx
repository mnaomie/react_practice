import { BrowserRouter } from "react-router-dom";
import {Router, Routes} from "router";

export function Header() {
    return (
        <div>
            <BrowserRouter path='/'/>Display
        </div>
    )
}